"""Pure, in-process core building blocks for MMCO.

Value types, the driver plugin contract, clock/offset math, the error-code taxonomy, the session
manifest model, and the log-event shape — all stdlib, no I/O, no processes. Everything the runtime
phases build on lives here.
"""
